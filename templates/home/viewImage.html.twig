
{% extends 'base.html.twig' %}

{% block title %}Mon espace{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 text-center pb-5">
                <h1 class="py-5">{{ originalFileName }}</h1>
                <img src="data:image/{{ format }};base64,{{ content }}" alt="{{ originalFileName }}" width="50%">
            </div>
        </div>
    </div>
{% endblock %}
